colors: false

pre-commit:
  parallel: true
  commands:
    quality-gate:
      glob: ["*", "**/*"]
      run: npm run check
      fail_text: "Quality gate failed — run `npm run check:fix` locally to auto-fix, then commit."
