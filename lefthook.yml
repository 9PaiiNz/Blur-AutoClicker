pre-commit:
  parallel: true
  commands:
    quality-gate:
      glob: ["*", "**/*"]
      run: npm run check
      fail_text: "Quality gate failed — run `npm run check` locally and fix issues before committing."
